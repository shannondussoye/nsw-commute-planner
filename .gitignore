# Python
venv/
.pytest_cache/
__pycache__/
*.pyc

# Environment
.env
otp.jar

# Data files (downloaded by scripts — large binaries, not tracked)
data/gtfs/
data/osm/
data/.manifest.json

# OTP build output (compiled graph — regenerated by build_graph.sh)
otp_data/

# Runtime
logs/*.log
.refresh.lock
